$(document).ready(function() {

	// 設備
	$.each($("[name=equipmentArray]"), function(i, element) {
		var index = i;
		$(element).next()[0].innerText
	});

	// こだわり条件
	$("[name=goodForConditionArray]")

});